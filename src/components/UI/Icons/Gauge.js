import React from 'react';
import gaugeIcon from '../../../assets/icons/gauge.svg';

const icon=(props)=>(
    <img src={gaugeIcon} style={{height: '60%', width: '60%'}}/>
    );
    
export default icon;